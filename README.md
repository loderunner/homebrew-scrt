# This is the Homebrew tap for [`scrt`](https://github.com/loderunner/scrt).

See https://github.com/loderunner/scrt for details.

## Install instructions

To install `scrt` using `brew`:

```shell
brew tap loderunner/scrt
brew install scrt
```
